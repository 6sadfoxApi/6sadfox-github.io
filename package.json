cat > package.json <<'EOF'
{
  "name": "6sadfox-github-io",
  "version": "1.0.0",
  "private": true,
  "description": "Static site with scraper backend and local preview server",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "openai": "^4.77.0"
  }
}
EOF

npm install

git add package.json package-lock.json
git commit -m "Add scraper backend dependencies"
git push origin main
